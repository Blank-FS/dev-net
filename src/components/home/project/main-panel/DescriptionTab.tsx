const DescriptionTab = () => {
  return (
    <div className="border w-full h-full mr-4">
      The description (i.e. purpose, features, goals, etc.) of the project will
      go here. Will probably use markdown for description display.
    </div>
  );
};

export default DescriptionTab;
